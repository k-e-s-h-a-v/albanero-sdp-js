let str = "Hello";
// Array.from converts an iterable into an array
console.log( Array.from(str) ); // [H,e,l,l,o]
console.log('~~~~~~~~~~~~~~~~')
//spread does the same thing
console.log( [...str] ); // [H,e,l,l,o]